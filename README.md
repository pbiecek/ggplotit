Just plot it with the ggplot2 package
=====================================

Installing ggplotit package:
To get started, install the latest version of **SmarterPoland**:
```{Ruby}
if (!require(devtools)) {
    install.packages("devtools")
    require(devtools)
}
install_github("pbiecek/ggplotit")
```
